dist: trusty
sudo: false
language: php

php:
  - 7.4snapshot

before_install:
  - composer self-update

jobs:
  include:
    - stage: Test
      php: 7.4snapshot
      install: travis_retry composer install --prefer-dist
      script:
        - ./vendor/bin/phpunit

    - stage: Code Style
      php: 7.4snapshot
      install: travis_retry composer install --prefer-dist
      script:
        - vendor/bin/phpcs

cache:
  directories:
    - $HOME/.composer/cache
